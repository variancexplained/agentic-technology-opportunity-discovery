# Skills

Skill definitions live here. Each skill is a markdown file with XML-tagged sections (role, shared_context, instructions, output_format, etc.) that load into an agent's system prompt on start.

Convention: one file per skill, kebab-case filename, content organized under XML tags so the model can attend to sections cleanly.

Starter kit layout is preserved so Claude Code discovery works out of the box.
