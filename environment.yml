name: project-name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - anthropic>=0.40.0
      - pydantic>=2.9.0
      - pydantic-settings>=2.5.0
      - python-dotenv>=1.0.0
      - pytest>=8.0.0
      - pytest-asyncio>=0.24.0
      - ruff>=0.6.0
